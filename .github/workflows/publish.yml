name: Publish release

on:
  workflow_dispatch:
  push:
    branches:
      - main

permissions:
  contents: write

jobs:
  publish:
    name: Tag, package, and publish release
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v5
        with:
          fetch-depth: 0

      - name: Read plugin metadata
        id: plugin
        shell: bash
        run: |
          set -euo pipefail

          VERSION="$(
            awk -F '=' '
              /^\[meta\]/ { in_meta = 1; next }
              /^\[/ { in_meta = 0 }
              in_meta && $1 ~ /^[[:space:]]*version[[:space:]]*$/ {
                gsub(/^[[:space:]]+|[[:space:]]+$/, "", $2)
                gsub(/^"|"$/, "", $2)
                print $2
                exit
              }
            ' info.next.toml
          )"

          NAME="$(
            awk -F '=' '
              /^\[meta\]/ { in_meta = 1; next }
              /^\[/ { in_meta = 0 }
              in_meta && $1 ~ /^[[:space:]]*name[[:space:]]*$/ {
                gsub(/^[[:space:]]+|[[:space:]]+$/, "", $2)
                gsub(/^"|"$/, "", $2)
                print $2
                exit
              }
            ' info.next.toml
          )"

          TURBO_VERSION="$(
            awk -F '=' '
              /^\[meta\]/ { in_meta = 1; next }
              /^\[/ { in_meta = 0 }
              in_meta && $1 ~ /^[[:space:]]*version[[:space:]]*$/ {
                gsub(/^[[:space:]]+|[[:space:]]+$/, "", $2)
                gsub(/^"|"$/, "", $2)
                print $2
                exit
              }
            ' info.turbo.toml
          )"

          if [[ -z "$VERSION" ]]; then
            echo "::error::Could not read [meta].version from info.next.toml"
            exit 1
          fi

          if [[ -z "$NAME" ]]; then
            echo "::error::Could not read [meta].name from info.next.toml"
            exit 1
          fi

          if [[ "$TURBO_VERSION" != "$VERSION" ]]; then
            echo "::error::Plugin manifest versions differ: Trackmania=$VERSION Turbo=$TURBO_VERSION"
            exit 1
          fi

          SLUG="$(echo "$NAME" | tr '[:upper:]' '[:lower:]' | sed -E 's/[^a-z0-9]+/-/g; s/^-+|-+$//g')"

          echo "version=$VERSION" >> "$GITHUB_OUTPUT"
          echo "tag=v$VERSION" >> "$GITHUB_OUTPUT"
          echo "name=$NAME" >> "$GITHUB_OUTPUT"
          echo "slug=$SLUG" >> "$GITHUB_OUTPUT"

      - name: Create tag on current commit
        shell: bash
        run: |
          set -euo pipefail

          TAG="${{ steps.plugin.outputs.tag }}"
          HEAD_SHA="$(git rev-parse HEAD)"

          if git rev-parse "$TAG" >/dev/null 2>&1; then
            git tag -f "$TAG" "$HEAD_SHA"
            git push origin "refs/tags/$TAG" --force
          else
            git tag "$TAG" "$HEAD_SHA"
            git push origin "refs/tags/$TAG"
          fi

      - name: Package plugin .op files
        id: package
        shell: bash
        run: |
          set -euo pipefail

          TRACKMANIA_ARTIFACT="${{ steps.plugin.outputs.slug }}-trackmania-${{ steps.plugin.outputs.tag }}.op"
          TURBO_ARTIFACT="${{ steps.plugin.outputs.slug }}-turbo-${{ steps.plugin.outputs.tag }}.op"
          bash scripts/build-op.sh all dist

          echo "trackmania_artifact=dist/$TRACKMANIA_ARTIFACT" >> "$GITHUB_OUTPUT"
          echo "turbo_artifact=dist/$TURBO_ARTIFACT" >> "$GITHUB_OUTPUT"

      - name: Create GitHub release
        env:
          GH_TOKEN: ${{ github.token }}
        shell: bash
        run: |
          set -euo pipefail

          TAG="${{ steps.plugin.outputs.tag }}"
          TITLE="${{ steps.plugin.outputs.name }} $TAG"
          TRACKMANIA_ARTIFACT="${{ steps.package.outputs.trackmania_artifact }}"
          TURBO_ARTIFACT="${{ steps.package.outputs.turbo_artifact }}"

          if gh release view "$TAG" >/dev/null 2>&1; then
            gh release upload "$TAG" "$TRACKMANIA_ARTIFACT" "$TURBO_ARTIFACT" --clobber
          else
            gh release create "$TAG" "$TRACKMANIA_ARTIFACT" "$TURBO_ARTIFACT" --title "$TITLE" --generate-notes
          fi
